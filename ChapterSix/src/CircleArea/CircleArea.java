package CircleArea;

public class CircleArea {

    public double CircleArea(double radius) {
//        double area = Math.random(Math.PI * Math.pow(radius,2));
        double circleArea=2*Math.PI*Math.pow(radius,2);
        return circleArea;
    }
}
